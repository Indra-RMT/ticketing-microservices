interface Color {
  red: number;
  blue: number;
  green: number;
  reds: number;
}

const color: Color = {
  red: 10,
  blue: 10,
  green: 10,
  reds: 11,
};

console.log(color);

export default color;
